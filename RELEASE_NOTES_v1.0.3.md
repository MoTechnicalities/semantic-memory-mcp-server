# semantic-memory-mcp-server v1.0.3

Release-coherence follow-up after `v1.0.2`.

## What Changed

- aligns the published package metadata with the public `v1.0.x` tag series
- preserves the same runtime API, benchmark artifacts, and federated MCP server behavior as `v1.0.2`

## Scope

This is a packaging and release-coherence release. No runtime behavior, MCP tool surface, or sample benchmark artifact changed from `v1.0.2`.