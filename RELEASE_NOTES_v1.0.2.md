# semantic-memory-mcp-server v1.0.2

Documentation coherence follow-up after `v1.0.1`.

## What Changed

- defines RWIF explicitly as Resonant Wave Information Format in the public README
- keeps the runtime API, benchmark artifacts, and federated MCP server behavior unchanged from `v1.0.1`

## Scope

This is a documentation-only release that aligns the latest tagged snapshot with the current public README on `main`.